# Ghostty

Run `stow ghostty` from the dotfiles root to link `Library/KeyBindings/DefaultKeyBinding.dict` into `~/Library/KeyBindings/`, then quit and reopen Ghostty.

The binding suppresses the macOS alert for Cmd-Ctrl-Down while Ghostty's workspace shortcut stays app-local. It also disables that paragraph-direction shortcut in other AppKit apps.
